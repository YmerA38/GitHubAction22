name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
          echo "hello worl"
      - name: Multiline script
        run: |
          node -v
          npm -v
      
    
